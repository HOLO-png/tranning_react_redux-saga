import React, { Component } from 'react';
import { withStyles } from '@material-ui/core';
import styles from './styles';

class AdminPageContainer extends Component {
   render() {
      return <div>AdminPageContainer</div>;
   } // create simple because it only use to distingush the routes other
}

export default withStyles(styles)(AdminPageContainer);
