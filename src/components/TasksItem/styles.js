const styles = () => ({
   cardTask: {
      marginTop: 20,
   },
   cardActions: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'flex-end',
      flexBasis: 'none',
   },
});

export default styles;
